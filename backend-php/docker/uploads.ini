; Raises PHP's upload caps enough to accept a full-size phone photo before
; UploadController re-encodes it down. post_max_size must stay above
; upload_max_filesize (it covers the whole multipart body, not just the file).
upload_max_filesize = 10M
post_max_size = 12M
